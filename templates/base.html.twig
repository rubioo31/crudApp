<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>{% block title %}CRUD{% endblock %}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- Enlace al archivo CSS -->
    <link rel="stylesheet" href="{{ asset('styles/style.css') }}">
    {% block extra_head %}{% endblock %}
</head>
<body>
    <header>
        <h1>{% block header_title %}CRUD{% endblock %}</h1>
        <nav>
            <!-- Enlaces de navegacion -->
            <a class="button" href="{{ path('product_index') }}">Productos</a>
            <a class="button" href="{{ path('category_index') }}">Categorias</a>
            <a class="button" href="{{ path('order_index') }}">ordenes</a>
        </nav>
    </header>
    <div class="container">
        {% block body %}{% endblock %}
    </div>
    <!-- Enlace al archivo JavaScript -->
    <script src="{{ asset('scripts/script.js') }}"></script>
    {% block extra_scripts %}{% endblock %}
</body>
</html>
